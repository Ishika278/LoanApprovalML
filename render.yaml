services:
  - type: web
    name: loan-approval-ml
    runtime: python
    plan: free
    rootDir: LoanApprovalML
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit_app.py --server.address 0.0.0.0 --server.port $PORT
    autoDeploy: true
